# مكتبة سيف العشيرة — سياق المشروع

فهرس مكتبة منزلية شخصية بواجهة عربية RTL. المالك مستخدم واحد. لغة العمل في
هذا المستودع العربية: التعليقات ورسائل الواجهة والتوثيق بالعربية، وأسماء
المعرّفات بالإنجليزية.

## المكدّس

React 18 + TypeScript + Vite للواجهة، و[Convex](https://convex.dev) للقاعدة
والدوال والمصادقة. لا SQL ولا Supabase (هُجر في التزام `afa6494`).

## الحالة الآن

الهجرة من Supabase إلى Convex مكتوبة بالكامل لكنها **لم تُشغَّل ولم تُختبر بعد**:
الجلسة التي كتبتها كانت في بيئة محجوبة الشبكة عن خوادم Convex. الخطوات
المتبقّية بالأوامر في `README.md`، وخلاصتها:

1. `npm install` ثم `npx convex dev` — ينشئ النشر ويولّد `convex/_generated/`
   (بدونه لا يمرّ `tsc`؛ هذا وضع طبيعي لأي مشروع Convex قبل أوّل تشغيل).
2. `npx @convex-dev/auth` ثم `npx convex env set OWNER_EMAIL <البريد>`.
3. إنشاء حساب المالك مرّة واحدة (بتبديل `flow` في `Login.tsx` مؤقّتًا).
4. استيراد الكتب من تصدير Supabase عبر `scripts/supabase-to-convex.mjs`.

أوّل ما يجب فعله في أي جلسة جديدة: تشغيل `npx convex dev` والتحقّق من أن
الموقع يعمل فعلًا قبل أي تطوير جديد.

## أوامر

```bash
npm run dev          # vite على 5173
npm run dev:backend  # convex dev — يراقب convex/ ويدفع التعديلات
npm run build        # tsc -b && vite build
```

## اصطلاحات ثابتة

- حقول الكتاب `camelCase` (`shelfLocation`, `readingStatus`, `publicationYear`).
- حالات القراءة ثلاث فقط، مصدرها الوحيد `convex/constants.ts`:
  «لم يُقرأ» / «قيد القراءة» / «انتهى».
- الحقول الاختيارية تُحذف عند الفراغ (`undefined`) ولا تُخزَّن نصًّا فارغًا.
- أنواع الواجهة مشتقّة من مخطط Convex (`Doc<'books'>`)، لا تُكتب يدويًّا.

## قرارات لها سبب — لا تعكسها بلا نقاش

- **`ctx.db.replace` لا `patch` في تعديل كتاب**: عميل Convex يُسقط الحقول ذات
  القيمة `undefined` أثناء النقل، فـ `patch` يُبقي القيمة القديمة عند مسح حقل
  اختياري.
- **`OWNER_EMAIL` في `convex/auth.ts`**: `profile()` يُستدعى في كل مسار مصادقة
  لا في التسجيل وحده، فرفض أي بريد سواه يمنع إنشاء حساب ثانٍ ودخوله معًا.
- **`migrate:importBooks` يرفض التنفيذ على مكتبة غير فارغة** إلا بـ `force`.

## عيوب معروفة لم تُصلح بعد

- رسالة خطأ الحفظ تُعرض في `Home.tsx` خلف النافذة المنبثقة، فيبدو أن الضغط على
  «حفظ» لم يفعل شيئًا. تُصلَح مع إعادة تصميم الواجهة.
- النقر على خلفية النافذة يغلق النموذج ويفقد المدخلات بلا تأكيد، ولا إغلاق
  بـ Esc ولا `autoFocus`.
- البحث لا يطبّع العربية: «احمد» لا تطابق «أحمد»، والتشكيل يكسر المطابقة.
- قائمتا التصفية بلا تسمية، وكلتاهما تعرض «الكل».
- `vite.config.ts` يبني على `base: '/Sword-of-the-Clan/'` (مسار GitHub Pages)
  بينما الموقع الحيّ على Firebase في الجذر — إن بقي كذلك فمسارات الأصول تفشل
  وتظهر صفحة بيضاء. القرار بين المنصّتين لم يُحسم بعد.

## خارطة الطريق

المالك يريد إعادة بناء جذرية بتوجيهه المفصّل، بعد أن أعجبته
`abuyarub-library.web.app` (مكتبة أبي يعرب). الفجوة عنها في العرض والتنظيم لا
في الهندسة، وأبرز ما ينقصنا:

1. **واجهة عامة**: الموقع كلّه اليوم خلف تسجيل الدخول. المطلوب صفحة عامة
   بترويسة فيها اسم المكتبة وصورتها واقتباس وموقعها، مع إبقاء الإضافة
   والتعديل للمالك وحده.
2. **إحصاءات**: عدد الكتب والمؤلفين والأقسام، وصفحة إحصاءات.
3. **تصفّح أغنى**: رقائق تصنيفات بدل قائمة منسدلة، شريط حروف هجائية للفهرسة،
   فرز بالنقر على رؤوس الأعمدة، تطبيع البحث العربي، وضع ليلي.
4. **حقول ناقصة**: الناشر (دار النشر)، فصل الدولاب عن الرفّ، حالة توفّر النسخة
   (متاح/معار) مستقلّة عن حالة القراءة، صفحة المؤلفين، صفحة «عن المكتبة».
5. **الجوال**: جدول من ثمانية أعمدة `nowrap` يُمرَّر أفقيًّا؛ الأنسب بطاقات
   دون 600px.
