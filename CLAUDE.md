# مكتبة سيف العشيرة — سياق المشروع

فهرس مكتبة منزلية شخصية بواجهة عربية RTL. المالك مستخدم واحد، والزوّار
يتصفّحون بلا حساب. لغة العمل في هذا المستودع العربية: التعليقات ورسائل
الواجهة والتوثيق بالعربية، وأسماء المعرّفات بالإنجليزية.

## المكدّس والنشر

React 18 + TypeScript + Vite، و[Convex](https://convex.dev) للقاعدة والدوال
والمصادقة والتخزين. والواجهة تُنشر ثابتًا على **استضافتين معًا** من الدفعة
نفسها، فهما متطابقتان أبدًا:

- **فايربيس** — <https://i-o-i-u.web.app> (الأقصر، وعليه المُعوَّل)
- **GitHub Pages** — <https://i-o-i-u.github.io/Sword-of-the-Clan/>

`.github/workflows/deploy.yml` ينفّذ `npx convex deploy --cmd 'npm run
build:deploy'` عند كل دفع إلى `main`: يدفع الدوال إلى نشر الإنتاج ثم يبني
الحزمتين، وهو نفسه يضبط `VITE_CONVEX_URL`. ويلزمه سرّان في المستودع:
`CONVEX_DEPLOY_KEY` و`FIREBASE_SERVICE_ACCOUNT`. وفايربيس يُنشر من وظيفة
البناء نفسها لأن `dist-root` على قرصها، وPages من وظيفةٍ ثانية لأن أداته
تشترط ذلك. وترويسُ التخزين المؤقّت في `firebase.json`: الحزمُ المبصومة
بالمحتوى سنةً، والخطُّ والصورُ أسبوعًا، وصفحةُ الدخول بلا تخزين — وإلّا لم
تُرَ نشرةٌ جديدة. **ولها قاعدتان: `/` و`/index.html` جميعًا**، لأن الترويس
يُطابَق على مسار الطلب والزائرُ يطلب `/`؛ فقاعدةٌ على `/index.html` وحده
لا تمسّه، ويبقى على افتراضيّ فايربيس — ساعةً كاملة لا تُرى فيها نشرة.

```bash
npm install
npx convex dev       # يراقب convex/ ويدفع التعديلات — لازمٌ قبل أي tsc
npm run dev          # vite على 5173
npm run build        # tsc -b && vite build
```

## بنية الشيفرة

- `convex/schema.ts` — الجداول والفهارس، **مصدر الحقيقة** للنموذج.
- `convex/privacy.ts` — الخصوصية: هويّة الطالب وحجب ما لا يراه الزائر.
- `convex/library.ts` — كل دوال القراءة. `books.ts` و`catalog.ts` الكتابة.
- `convex/auth.ts` — مصادقة كلمة السرّ بقيد `OWNER_EMAIL`.
- `src/lib/api.ts` — **الحدّ الوحيد مع Convex للبيانات** (٣٩ دالة).
- `src/lib/library.tsx` — حالة المكتبة كلها في سياق واحد.
- `src/lib/router.ts` — موجِّه بالتجزئة (`#/browse`, `#/book/:id`,
  `#/publisher/:id`, …).
- `src/views/` — `Landing`, `About`, `Browse`, `BookDetail`, `Authors`
  (وفيه `AuthorPage`، وهي صفحةُ الشخص الواحد مؤلِّفًا كان أو محقِّقًا)،
  `AddBook`, `Stats`, `Publishers` (وفيه `PublisherPage`)، ثم الصفحات
  الثلاث التي لا تُعرض في الرأس: `People`, `Series`, `Perks` (وفيه
  `PerkPage`، وهي صفحةُ القيد الواحد).
- `src/lib/people.ts` — قراءةُ الصفات من الكتب: ما ألَّفه الرجل، وما عمل
  فيه بكل صفة، وأصحابُ كل صفة في المكتبة.
- `src/lib/poetry.ts` — قسمةُ نصّ القيد كتلًا: نثرًا وشعرًا ورجزًا.
- `src/lib/tatweel.ts` — مدُّ الشطر بالتطويل، منقولٌ عن مدوّنة صاحب المكتبة.
- `src/lib/perks.ts` — قراءةُ القيود: ما يُشتقّ منها من أبوابٍ وأعلامٍ
  وكرّاساتٍ ووسوم، وترشيحُها وترتيبُها.
- `src/lib/citation.ts` — الإحالةُ إلى الكتاب وإلى القيد. مشتركةٌ بين بطاقة
  الكتاب والكنّاش، فلا تفترق صياغةُ العزو بين الموضعين.
- `src/lib/isbn.ts` — الردمك: تشريطُه وحسابُ خانة تحقُّقه وبلدُ مجموعته.
  حسابٌ محضٌ بلا نداءٍ خارجيّ.
- `supabase/schema.sql` — **مرجع تاريخي لا يُنفَّذ**. يبقى لأن تعليقاته تشرح
  النموذج ونيّة كل حقل.

## اصطلاحات ثابتة

- **حقول البيانات `snake_case`** (`author_id`, `series_no`, `year_era`). بقيت
  على حالها بعد الهجرة عمدًا: الواجهة كلها تقرؤها، وتغييرها كان سيحوّل تبديل
  الخلفية إلى إعادة كتابة.
- **النصوص العربية في `types.ts` هي نصوص الإنتاج نفسها**، مطابقة لوثيقة
  التسليم: لا تُترجم ولا يُعاد صوغها. ومنها `STATUSES`
  («لم يُقرأ» / «قيد القراءة» / «مقروء»). ولفظان متروكان بطلبٍ صريح من صاحب
  المكتبة: «مقروء» بدل «تم القراءة»، و«لم يُقرأ» بدل «لم تُقرأ» — الفاعلُ
  الكتابُ لا الصفحة. والقديمان باقيان في اتحاد `readingStatus` في المخطّط
  لأن في القاعدة مستنداتٍ كُتبت بهما، و`toClient` يحوّلهما عند القراءة فلا
  تراهما الواجهة. ولحالة القراءة وجهٌ رابع: الفراغ، أي «غير معروفة»، يرفعها
  صاحب المكتبة من بطاقة الكتاب.
- المعرّفات: `_id` في Convex يُعرض للواجهة باسم `id`، و`_creationTime` باسم
  `created_at` نصًّا ISO. المحوِّل `toClient` في `privacy.ts`.

## قرارات لها سبب — لا تعكسها بلا نقاش

- **`api.ts` هو الحدّ الوحيد مع Convex**: لا تستورد `convexClient` في ملف
  جديد. خارجه لا يمسّ Convex إلا `main.tsx` (المزوِّد) و`library.tsx`
  و`LoginOverlay.tsx` للجلسة. هذا الحدّ هو ما جعل هجرة الخلفية تبديلَ ملف.
- **الخصوصية في الخادم لا في المتصفّح**: كل دالة قراءة تسأل `isOwner(ctx)`
  بنفسها. لا تضف دالةً تعيد بيانات خامًا معتمدةً على ترشيحٍ في الواجهة.
- **وسيط `owner` في دوال `api.ts` بلا أثر**: أُبقي كي لا تتغيّر مواضع
  النداء، والخادم لا يصدّق دعوى العميل أنه المالك أصلًا.
- **ما كانت Postgres تفعله تلقائيًّا يُكتب صراحةً**: الحذف المتسلسل في
  `books.remove`، ومزامنة `author_name` في `catalog.updateAuthor`. إغفال
  أحدهما يترك مستنداتٍ يتيمة أو اسمًا قديمًا على الكتب.
- **الموجِّه بالتجزئة لا بالمسار**، و**`base` = `/Sword-of-the-Clan/` عند
  البناء**: شرطا النشر الثابت على Pages. **والموقع يُنشر على استضافتين**،
  فلكلٍّ حزمتُها بأساسها من الدفعة نفسها: `npm run build` يُخرج `dist`
  بأساس مسار المستودع لـPages، و`npm run build:root` يُخرج `dist-root`
  بأساس `/` لاستضافة فايربيس (`i-o-i-u.web.app`)، ويجمعهما `build:deploy`.
  و`convex deploy --cmd` يغلّفهما جميعًا فتُدفع الدوالُّ مرّةً واحدة.
- **موضع الكتاب رقمُ دولابٍ ورقمُ رفّ** (`cabinet_no`, `shelf_no`)، لا اسمًا
  يُختار من قائمة أرففٍ تُدار في الإعدادات. ودواليبُ صفحة التصفُّح تُشتقّ من
  الكتب نفسها، فلا جدول لها. جدول `shelves` بقي مُعرَّفًا لصفّ بذرةٍ فيه لا
  غير — يُحذف متى حُذف صفُّه.
- **بلد الدار في `publishers` لا في الكتاب**: يُكتب مرةً أولى ثم يُملأ منه
  حقلُ الكتاب ويُقفل، ولا يُعدَّل إلا من صفحة الدار. وتعديلُ اسم الدار أو
  بلدها يُزامَن على كتبها في `catalog.updatePublisher` كما يُزامَن اسم
  المؤلِّف — إغفالُه يترك على الكتب اسمًا قديمًا. و«مكان النشر» صار
  **«بلد النشر»** في كل ما يُعرض؛ اسمُ الحقل `place` على حاله.
- **دُوْر النَّشْر صفحتان كصفحتَي المؤلِّفين**: فهرسٌ ببطاقةٍ لكل دار
  (`#/publishers`)، وصفحةٌ لكل دار (`#/publisher/:id`) فيها شعارُها
  (`logo_url`) وبياناتُها وكتبُها. والتعديل في صفحة الدار وحدها؛ بطاقةُ
  الفهرس عرضٌ لا نموذجُ إدخال.
- **وفاة المؤلِّف تُدخَل من نموذج الكتاب** وإن كانت من بيانات المؤلِّف: عليها
  يقوم ترتيب المكتبة. ولها ثلاث حالات (`alive` / `death` / `death_approx`
  و`death_text`) لأن كثيرًا من القدماء لا تُعرف وفاتُهم إلا تقريبًا.
  و`setAuthorDeath` لا يمحو وفاةً مثبتة بإدخالٍ فارغ.
- **صفحة الكتاب عرضٌ لا تعديل**: لا يُعدَّل فيها إلا التقييم وحالة القراءة
  (وما ليس من بياناته: الفوائد والإعارة). وقلمُ التعديل في أعلاها يفتح
  `AddBook` نفسه مملوءًا (`#/edit/:id`)، فسلوكُ كل حقلٍ مكتوبٌ مرةً واحدة —
  منتقي السنة، وتحويل رقم الطبعة، وقفل مكان الدار، وصفوف المؤلِّفين. لا
  تُعِد بناء حقول التعديل في البطاقة.
- **كَعْبُ الكتاب صورةٌ واحدة لا صورةٌ لكل مجلَّد**: كعوب مجلَّدات الطبعة
  الواحدة لا تختلف إلا برقمها، فتُرفع مرةً وتُكرَّر على الرفّ ويُوضع على كلٍّ
  منها رقمُ مجلَّده في دائرة. و`spine_images` باقٍ سجلًّا كما في المخطّط،
  مفتاحُه `"1"` لا غير؛ وعرضُ الأرفف يقرأ المفاتيح القديمة احتياطًا.
- **لكلّ مشارِكٍ نطاقُ عمله** (`contributor.scope`): قد يُحقِّق المجلَّدَ
  الأولَ رجلٌ والثانيَ غيرُه، فيُكتب لكلٍّ ما عمل فيه نصًّا («١-٣»، «السِّفر
  الأول»). والفراغُ فيه معناه: عمل في الكتاب كلِّه. ومتى اختلفت النطاقاتُ في
  صفةٍ واحدة لم تُعطف أسماؤها بالواو في البطاقة — هؤلاء ليسوا شركاء في عملٍ
  واحد، بل لكلٍّ عملُه.
- **تاريخ الوُرود يقبل اليوم، وسنةُ النشر لا تقبله**: يومُ الطبعة لا يُعرف
  أصلًا، ويومُ الوُرود يُعرف. فمنتقي `HijriYearPicker` يفتح شبكةَ الأيام
  بوسيط `withDay`، ويُحفظ اليوم في `acquired_day`.
- **الكلماتُ المفتاحية غير الوُسوم**: الوسمُ خبرٌ يُعرض على البطاقة،
  والكلمةُ المفتاحية سبيلٌ إلى الكتاب في البحث لا تُعرض. ولذلك تمرّ
  `keywords` في `redactBook` بلا حجب: حجبُها يُعمي البحثَ ولا يستر شيئًا.
- **تفصيل المجلَّدات في نموذج الكتاب**: لكل مجلَّد صفحاتُه (`volume_pages`)،
  وما اشتمل عليه من أسفار المؤلِّف نصًّا (`volume_parts`)، وهل هو مجلَّد
  فهارس (`index_volumes`). ومجلَّد الفهارس **لا تُحسب صفحاتُه في الإجمالي**
  — فهرسٌ لا متن — ويتكفّل بذلك `sumVolumePages`. والقوائم الثلاث تُحفظ
  بطول عدد المجلَّدات تمامًا، فموضعُ كل قيمةٍ هو رقمُ مجلَّدها؛ ترشيحُ
  الفارغ منها يُزيح ما بعده.
- **صفة الوُرود وتصنيفُ الكتاب يجوز أن يُتركا**: الفراغ خيارٌ قائم في
  كليهما، فلا يُلزَم الفاهرس بما لا يعرف.
- **بطاقة الكتاب مقسومةٌ بأقسام النموذج نفسها** (بيانات الكتاب، فالطبعة،
  فالنسخة، فعنه)، لكلٍّ أيقونتُه. ومفاتيح صفوفها هي مفاتيح `META_DEFS`
  نفسها، فما أخفاه صاحب المكتبة من حقولٍ يبقى مخفيًّا. ولا يُعرض فيها
  صفرٌ ولا فراغ، ولا «اللغة: العربية» — اللغة خبرٌ حين تكون غيرَها.
- **صفةُ المشاركين تُثنَّى وتُجمع بعدد أصحابها** (`contributorLabel`):
  «المُحقِّقان: فلان وفلان»، «المُحقِّقون: …». وكلُّ صفةٍ سطرٌ على حِدَة، فلا
  يُقال «المحقِّق ومن معه». و«تَقْرِيظ» و«تقديم» مصدران لا يُثنَّيان ولا
  يُجمعان مهما كثر أصحابُهما.
- **الأرقام تُعرض بفاصلةٍ بين كل ثلاث مراتب** (`formatNumber`): «1,130».
  ولا تُطبَّق على السنين — «١٤٤٧ هـ» لا «1,447».
- **لا يُوصَل عددٌ باسمٍ إلا من `countLabel`**: «١ كتابًا» لحن، والصواب
  «كتابٌ واحد» ثم «كتابان» ثم «٣ كتبٍ» ثم «١١ كتابًا». وصيغُ كلِّ معدودٍ في
  ثابتٍ باسمه (`BOOKS_COUNT`, `VOLUMES_COUNT`, …). فمتى عُرض عددٌ مع اسمه
  فمن هنا لا بقالب نصّي.
- **كلُّ نصٍّ معروضٍ يزيد على سطرين يُضبط طرفاه** بصنف `.prose`: النبذةُ
  والملاحظاتُ والطُّرَّةُ والفوائدُ وترجمةُ المؤلِّف وملاحظاتُ الدار ونتيجةُ
  الحاسبة. وضبطُ الطرفين هو ما تُطبع به الكتب العربية. وحقولُ الإدخال
  مستثناة — ما يُكتب لا يُضبط.
- **بطاقة الكتاب لها أدواتٌ ثلاث في ذيلها** لا تُعدِّل شيئًا: نسخُ رابطها،
  وطبعُها، ونسخُ إحالتها لجريدة المراجع (`citationOf`). وهي للزائر كما هي
  لصاحب المكتبة.
- **الرابط المختصر `#/b/xxxxxx`**: معرّفات Convex لا تُنسخ في رسالةٍ ولا
  تُملى، فيُقتطع منها في `shortBookId` أقصرُ بادئةٍ لا يشاركه فيها كتابٌ
  آخر. وصفحةُ الكتاب تقبل البادئة كما تقبل المعرّف التامّ، فلا يضيع رابطٌ
  قديم.
- **ورقةُ الطباعة تُركَّب في `document.body`** لا في شجرة الصفحة
  (`createPortal`): الطباعةُ تطوي `#root` كلَّه بـ `display: none`، ولو
  كانت داخله لاختفت معه. ولو أُخفيت الصفحةُ بالشفافية لبقيت شاغلةً مواضعها
  فخرجت أوراقٌ بيضٌ قبل الورقة وبعدها.
- **صفحتا المؤلِّف والدار عرضٌ حتى يُضغط القلم**، كصفحة الكتاب: لا يرى
  الفاهرسُ نموذجَ إدخالٍ وهو إنما جاء ليقرأ. والحقول تُفتح بحالةٍ محلّية
  (`editing`) لا بمسارٍ جديد، لأن الحفظ فيها فوريٌّ مؤجَّل لا بزرّ.
- **الإحصائيات ثلاث طبقات**: بطاقاتُ أعدادٍ مفردة، فمفاخرُ (أكثرُ عنوانٍ
  مجلَّدات، وأغلى كتاب، وأكثرُ التصنيفات)، فقوائمُ الأكثر والأقدم. وقاعدةُ
  الصفحة: **ما كان فارغًا لا يُعرض** — بطاقةٌ تقرأ صفرًا ليست خبرًا.
- **نصّ «عن المكتبة» في `types.ts` (`ABOUT_TEXT`)** أصلًا لا في القاعدة:
  الزرعُ الأوّل كتب في `about_text` نصًّا تجريبيًّا، و`aboutTextOf` يعدّه
  فراغًا فيحلّ محلَّه النصُّ المعتمَد. **وحقلُ الإعدادات يُملأ بـ`aboutTextOf`
  لا بالمحفوظ الخام**، فيرى صاحبُ المكتبة النصَّ المعروضَ نفسَه ويعدّله منه،
  فيُحفظ عندئذٍ في القاعدة ويسقط الافتراضيّ. وعارضُ الصفحة يقسم النصّ على
  وجوهه بأماراتها — البسملةُ، فالمتنُ، فالخاتمةُ، فالتوقيع — لا بمواضعها،
  فيبقى التنسيق قائمًا وإن غُيِّر النصّ. **ولا حرفَ مُصدَّرًا** في أوّل
  الفقرة: تلك صنعةُ الطباعة اللاتينية لا يستعملها العرب.
- **الحمل الأوّل**: لا خطوط جوجل حاجبة في `index.html` (أميري وحده غيرَ حاجب،
  وبقيّةُ العائلات تُجلب عند اختيارها من `ensureWebFont`)، والصفحات الثانويّة
  مقسومة بـ`lazy`. **والتحميل يبدأ مع أوّل رسمٍ لا ينتظر جلسةً ولا دورًا**:
  انتظارُ `authLoading` كان يُقحم إجراءَ `auth:signIn` — تجديدَ الرمز من
  `localStorage` — جولةً كاملةً في المسار الحرج، فيبطؤ دخولُ صاحب المكتبة
  دون تصفُّح الزائر، ويزداد كلّما أُغلق المتصفّح إذ يموت الرمزُ العامل بموت
  الصفحة. فالآن تُطلب البيانات بهويّة زائرٍ فورًا، ويُعاد التحميل بهويّة
  المالك متى استقرّ دورُه (`loadedAsOwner`). ولأن التحميلات صارت تتسابق
  فلكلٍّ رقمُ نوبةٍ (`loadSeq`) لا يُقبل إلا جوابُ آخرِها، وما بعد الأوّل
  يُحمَّل في صمتٍ فلا تُمحى الشاشة.
- **صفحة الهبوط تُرسم قبل وصول البيانات**: حجبُ `loading` في `App.tsx` يعمّ
  ما سواها لا هي — إطارُها وشعارُها وأزرارُها لا تنتظر شيئًا. وما سواها
  يُحجب عمدًا: صفحةٌ تقول «لا توجد كتب» ثم تمتلئ أسوأُ من انتظارٍ صريح.
- **خطّ `Kitab`** في `public/fonts/Kitab-Regular.woff2`، يُسجَّل من `theme.ts`
  لا من ملف الأنماط لأن مساره يتبع مسار النشر. حذفُه يُرجع الواجهة إلى Amiri.
  وكان `.ttf` (٢٢٠ ك.ب) فحُوِّل إلى woff2 (١٠٢ ك.ب) — والمصدرُ في تاريخ git.
- **حدُّ خطأٍ فوق الشجرة كلِّها** (`components/ErrorBoundary.tsx` في
  `main.tsx`، خارج المزوِّدات): استثناءٌ واحدٌ في الرسم كان يُسقط React كلَّه
  فتبقى الصفحة بيضاء بلا كلمة. وقد وقع: `condition_notes` اختياريٌّ في
  المخطّط، و`catalogScore` ينادي `.trim()` عليه، والشارةُ **لصاحب المكتبة
  وحده** — فكانت صفحةُ التصفُّح تُمحى عليه دون الزائر. **فكلُّ نصٍّ يُقرأ من
  مستندٍ يمرّ بحارس**: الحقولُ المستجدّة تُعرَّف اختياريّةً بالضرورة، فالغيابُ
  حالٌ قائمة لا شذوذ.
- **جلسة صاحب المكتبة تموت بإغلاق اللسان** (`storage={sessionStorage}` على
  `ConvexAuthProvider`): قرارٌ صريح منه — لا يبقى الدخولُ بين الزيارات، ويُعاد
  في كل مرّة. وتحديثُ الصفحة لا يُخرجه، فالرمزُ باقٍ ما دام اللسان مفتوحًا.
- **`person_id` اختياريٌّ في `contributor`**، فمشارِكو الكتب المفهرَسة قبله
  أسماءٌ بلا سجلّ، و`peopleByRole` يُسقط من لا معرّف له — فتظهر «المحقِّقون
  ونحوهم» فارغةً وفيها محقِّقون. ويربطهم `maintenance.linkContributors`
  بأسمائهم، ويُشغَّل مرّةً ويُعاد بلا ضرر.
- **مدخل صاحب المكتبة مخفيّ**: لا زرَّ دخولٍ في الرأس، بل ثلاث نقراتٍ على
  صورة خلفية صفحة الهبوط تفتح `LoginOverlay`. هذا طلبٌ صريح: الزائر لا يرى
  بابًا لا يخصّه. لا تُعِد زرّ الدخول إلى الترويسة.
- **صور الهبوط واقتباساته جدولان منفصلان** (`landing_images`,
  `landing_quotes`): كلٌّ يدور بمهلته (`rotate_seconds` و`quote_seconds`).
  كانا جدولًا واحدًا (`landing_slides`) يقرن الصورة باقتباسها، وبقي معرَّفًا
  في المخطّط فارغًا بعد أن نقل `seed:run` صفوفه — يُحذف متى شئت.
- **الحقول المستجدّة في `library_settings` اختياريّة في المخطّط**: مستند
  الإعدادات واحدٌ قائمٌ من قبل، وإلزامُها يُفشل تحقّق المخطّط عليه.
  `loadSettings` يسدّ الناقص من `DEFAULT_SETTINGS` في الخادم، و
  `DEFAULT_SETTINGS_EXTRAS` في `api.fetchSettings` يسدّه في الواجهة.

- **الخصوصية على ثلاث درجات، وحكمُها في `privacy.fieldHidden`**: مستندٌ
  بعينه (`hidden_book_ids`, `hidden_author_ids`, `hidden_publisher_ids`)،
  فحقلٌ من مستندات النوع كلِّها (`hidden_fields`, `hidden_author_fields`,
  `hidden_publisher_fields`) ومعه استثناءٌ يردّ مستندًا إلى الظهور
  (`*_field_exceptions`)، فحقلٌ من مستندٍ بعينه (`*_field_overrides`).
  **وأخصُّ الأحكام أولاها**: ما أُخفي من مستندٍ بعينه مخفيٌّ وإن لم يُخفَ
  من نوعه. ومعرّفاتُ هذه القوائم `v.string()` لا `v.id`: معرّفٌ ميّت في
  قائمةٍ يُفشل تحقّقَ المخطّط على مستند الإعدادات كلِّه، وهو لا يضرّ إذ لا
  يُقرأ منه إلا ما طابق موجودًا.

- **ما لا يُخفى أصلًا**: عنوانُ الكتاب، واسمُ مؤلِّفه، واسمُ المكتبة، واسمُ
  صاحب الترجمة في بطاقته، واسمُ الدار في بطاقتها. إخفاؤها يُفسد الفائدة من
  الفهرس، فلا تُعرَض في تبويب الزوار أصلًا. **واسمُ الدار يُخفى من بيانات
  الكتب**: يسقط عندئذٍ `publisher_id` معه فلا يبقى الكتابُ منسوبًا إليها،
  وإن أُخفي من كتبها كلِّها لم تُعرض الدارُ — لا معنى لدارٍ لا كتاب لها.

- **إخفاءُ المؤلِّف يُخفي كتبَه، والعكسُ ليس بلازم**: `bookIsPublic` يردّ
  كتبَ المخفيّ، و`library.authors` لا يعرض إلا من بقي له كتابٌ ظاهر — فمن
  أُخفيت كتبُه كلُّها سقط اسمُه معها. **والأعدادُ العامّة لا تُخفى** وإنما
  تسقط منها حسبةُ ما أُخفي: كلُّ رقمٍ في `Stats` مشتقٌّ من `books` وهي
  المُرشَّحة في الخادم.

- **سجلُّ الأشخاص واحد**: جدولُ `authors` نفسه يحمل المؤلِّفَ والمحقِّقَ ومن
  على صفتهما، ويحمل كلُّ `contributor` معرّفَ صاحبه فيه (`person_id`).
  فالرجلُ الواحد — كمحمود شاكر — له سجلٌّ واحد وصفحةٌ واحدة
  (`#/author/:id`) تُعرض فيها مؤلَّفاتُه ثم أعمالُه بصفاته على ترتيب
  `CONTRIBUTOR_ROLES`. **وصفحةُ المؤلِّفين لا تعرض إلا من له تأليفٌ مسجَّل**،
  و«المحقِّقون ونحوهم» (`#/people`) تعرض من له عملٌ بصفة، ويجتمعان فيمن
  جمعهما. ودوالُّ القراءة في `src/lib/people.ts`.

- **لكلّ صفةٍ ثلاثةُ ألفاظ، ومَرْجِعُها `types.ts` وحده**: نعتُ صاحبها مفردًا
  (`ROLE_PERSON` — «المُحقِّق»، و«صاحب التقريظ» لأن «تقريظ» مصدرٌ لا يُنعَت
  به رجل)، ومصدرُ أعماله (`ROLE_WORKS` — «تحقيقاته»، «اعتناءاته»،
  «تصحيحاته»، «تخريجاته»، «ترجماته»، «تقريظاته»)، وجمعُ أصحابها
  (`roleGroupLabel` — «المحقِّقون»، و«أصحاب التقريظ»). والتأليفُ صفةٌ منها
  (`AUTHOR_ROLE`) وإن لم يكن في `CONTRIBUTOR_ROLES`.
- **الرجلُ يُنعَت بأعلى صفةٍ سُجِّلت له** (`people.topRole` على `ROLE_RANK`:
  التأليفُ فوق الكلّ، ثم `CONTRIBUTOR_ROLES` على ترتيبها). فمن له تأليفٌ
  واحد فمؤلِّفٌ أبدًا، ومن حقّق واعتنى فمحقِّق. **والعلوُّ يحكم اللفظَ المفرد
  وحده** — «تعديل بيانات المُحقِّق» — **ولا يُسقط ما دونه**: الرجلُ يُعرض في
  صفِّ كل صفةٍ سُجِّلت له في «المحقِّقون ونحوهم»، وتُقسَم أعمالُه في صفحته
  بصفاته كلِّها بلا خلط — مؤلَّفاته، فتحقيقاته، فاعتناءاته.
- **صياغةُ صلة الكتاب بأصله من `WORK_PHRASES` في كل موضع**: حرفُ الجرّ يتبع
  نوع العمل ولا يُقاس («انتقاءٌ من» لا «انتقاءٌ على»). ويقرؤها نموذجُ
  الإضافة كما تقرؤها بطاقةُ الكتاب — قائمةُ الاختيار ورقاعُ الصلات جميعًا —
  فلا يفترق اللفظُ بين موضع الإدخال وموضع العرض.
- **التصنيفات رئيسٌ وفروع في جدولٍ واحد**: الفرعُ صفٌّ فيه `parent` اسمُ
  رئيسه، والاسمُ فريدٌ في الجدول كلِّه. والكتابُ يحفظ الاثنين
  (`category`, `sub_category`) لا الفرعَ وحده: نقلُ فرعٍ من رئيسٍ إلى آخر
  لا يغيّر نسبةَ كتبٍ ماضية. وحذفُ الرئيس يحذف فروعَه، وإخفاؤه يُخفيها.

- **المجلَّدات الناقصة لكلٍّ سببُه** (`missing_volumes`): قد يتلف الثاني
  وتضيع إعارةُ السابع، فلكلِّ مجلَّدٍ ناقصٍ رقمُه وسببُه على حِدَة. والسببُ
  يجوز أن يُترك فيُكتفى بـ«ناقص». ويُعلَّم النقصُ في سطر المجلَّد نفسه من
  تفصيل المجلَّدات، لا في قائمةٍ تُعاد فيها الأرقام. وخبرُه يُصاغ مطابقًا
  لمعدوده في `missingVolumesHeadline` — «ناقصٌ منه مجلَّدٌ واحد»،
  «ناقصان منه»، «ناقصةٌ منه ٣ مجلَّداتٍ».

- **نسبةُ نقص الفهرسة في بطاقة الشبكة وحدها** (`catalogScore`): حلقةٌ
  تمتلئ بقدر ما أُدخل من حقول `META_DEFS` مع الغلاف والنبذة والتصنيف،
  بوزنٍ واحد. **لصاحب المكتبة وحده** — هي تذكيرٌ له بما بقي عليه لا خبرٌ
  عن الكتاب — ولا تُعرض على الكتاب التامّ: التمامُ هو الأصل والخبرُ في
  النقص. والعنوانُ واسمُ المؤلِّف خارج الحساب: لا يُحفظ كتابٌ بغيرهما.

- **صندوقُ الفوائد لا يُعرض فارغًا**: بطاقةٌ تُخبر أن لا فائدة فيها ليست
  خبرًا. ويبقى لصاحب المكتبة زرٌّ مفرد يفتح النموذج، لا صندوقٌ كامل.

- **ثلاثُ صفحاتٍ لا تُعرض في الرأس أبدًا**: «المحقِّقون ونحوهم»
  و«السلاسل» و«الفوائد والمقتطفات». الرأسُ للطريق الذي يسلكه كل زائر،
  وهذه يُقصَد إليها قصدًا. ومدخلُها من داخل «تصفُّح المكتبة» إلى جانب
  الحاسبة، ومن «عن المكتبة» تحت الإحصاءات — والقطعةُ واحدة
  (`components/SideDoors.tsx`) كي لا يفترق الاسمُ والأيقونة بين الموضعين.
  والسلاسلُ تُشتقّ من الكتب نفسها لا من جدول، كما تُشتقّ الدواليب.

- **بطاقةُ الاقتباس بحجمٍ ثابت** (`min-height` في `.quote-card`)، ونصُّها
  موسَّطٌ لا مضبوط الطرفين: لو تبعت طولَ النصّ لقفزت الصفحةُ عند كل تبديل.
  ولذلك حدٌّ أقصى للحروف في نافذة الإعدادات (`QUOTE_MAX_CHARS`) مقدَّرٌ
  بارتفاع البطاقة.

- **موضع المكتبة خبرٌ واحد** (`show_landing_place`): يظهر في صفحة الهبوط
  وفي «عن المكتبة» جميعًا، ويُخفى عنهما جميعًا — لا يُستر في صفحةٍ ويُكشف
  في أخرى.

- **نموذجُ إضافة الدار لا يُعرض حتى يُطلب**: صفحةُ الدُّور فهرسٌ يُقرأ،
  وزرُّ الزائد بعد البطاقات — في هيئتها لا زرًّا غريبًا عنها — هو ما يفتحه.


- **ورقُ الجدران طبقةٌ واحدة تحت الموقع كلِّه** (`body::before` للنقش،
  و`body::after` للهالتين)، **لا نقشًا مبثوثًا في جوف الألواح**. وثلاثةُ
  شروطٍ فيه لا تُخالَف، وبمخالفتها قبُح أوّلَ مرّة:
  ١. **تحت المحتوى**: رتبتُه سالبة، ولونُ الأرض على `html` لا على `body` —
     ولو كان على `body` لنُقل إلى لوحة الرسم فلم يُدرَ أفوقه هو أم تحته.
     وكلُّ لوحٍ في الموقع أرضُه مصمتة فلا ينفذ النقشُ إلى وجهه. وكان في
     جوف إطار الهبوط وبطاقةِ الاقتباس وصدرِ الكنّاش، فيُرى كأنه مطبوعٌ على
     الصورة وعلى النصّ. **فلا تُعِده إلى جوف لوحٍ أبدًا.**
  ٢. **ثابتٌ لا يجري مع التمرير**، فالجدارُ لا يتحرّك خلف النافذة.
  ٣. **باهتٌ جدًّا** (٤٫٥٪ في الفاتح، ٧٫٥٪ في الداكن). وهو مرسومٌ بالقناع
     فيُصبَغ بلون المكتبة ويتبع المظاهرَ الثلاثة. ولا يُطبع.
- **رموزُ الزينة في `:root` لا في كل موضع** (`--shadow-1..3`, `--ease`,
  `--ease-spring`, `--ornament`): كان كلُّ لوحٍ يجتهد لنفسه في ظلِّه ومنحنى
  حركته، فتختلف الأعماقُ في الصفحة الواحدة بلا سبب. و`--ornament` قناعُ
  «ربع الحزب» محرَّفًا في ملف الأنماط لا ملفًّا يُجلب، ويُصبَغ بلون المكتبة
  فيتبع المظاهرَ الثلاثة بلا نسخةٍ لكلٍّ منها. وهو الذي يزيّن أرضَ الهبوط
  وإطارَ الصورة وبطاقةَ الاقتباس وصدرَ الكنّاش والصفحاتِ الخالية.
- **`--accent` يُرفع في المظهر الداكن من ملف الأنماط لا من `theme.ts`**:
  `applyTheme` يكتب رموزَه في نمطٍ سطريّ على الجذر، والنمطُ السطريّ يغلب
  المظاهرَ الثلاثة جميعًا فلا يُخصَّص منه واحد. فقاعدةُ
  `:root[data-theme='dark']` في `styles.css` هي موضعُ ما يخصّ الداكن.
- **صورةُ الهبوط لا تُركَّب كلُّها دفعةً واحدة**: المعروضةُ وحدها في أوّل
  رسم، ثم تُركَّب التي تليها **بعد أن تفرغ الأولى من الجلب** (`loaded`)،
  فلا تتقاسم خمسُ صورٍ عرضَ الشبكة وتبطؤ أُولاها وهي وحدها المعروضة. ولا
  يُنتقل بالدوران إلا إلى صورةٍ وصلت، وإخفاقُ الجلب يُعدّ وصولًا كي لا يقف
  الدورانُ على صورةٍ لا تجيء. و`fetchpriority` يُكتب صغيرًا كما في HTML:
  React 18 لا يعرف الصيغة الكبيرة فيُسقطها من العنصر إسقاطًا.
- **الاقتباس يتبدّل على مرحلتين**: يخفت المعروضُ، فإذا خفت بُدِّل النصُّ
  وأُصعد الجديد. وكان القديمُ يُرفع في اللحظة ويُبدأ بإظهار الجديد، فيُرى في
  وسط البطاقة فراغٌ ثم قفزة. وتحت البطاقة خيطٌ يقطعها بمقدار ما بقي من
  المهلة، ويقف متى وقف المؤشِّر عليها — فالتبديلُ متوقَّعٌ لا يُفاجئ القارئ
  في وسط سطر.

- **النوافذ كلُّها ثلاثةُ أثلاث، ولا تُقاس أطوالُها بـ`vh` أبدًا**:
  `.overlay-wrap` فيها `overflow-y: auto`، و`.overlay-sheet` فيها
  `max-height: 100%` — والطبقةُ مثبَّتةٌ على الشاشة (`inset: 0`) فحدُّها
  حدُّها، وحاشيتُها مطروحةٌ منه. أمّا `vh` فلا تتبع `zoom` الذي على `#root`،
  فتخرج النافذةُ عن الشاشة كلّما كبّر القارئُ الخطّ. وذيلُ النافذة —
  حيث زرُّ الحفظ — **يكون خارج الجوف المُمرَّر لا لاصقًا داخله**، وإلّا وقع
  تحت حافّة الشاشة فلم يُبلَغ. وقد وقع ذلك في نموذج القيد.
- **صدرُ الهبوط لا ينضغط دون محتواه** (`.hero { flex: 1 0 auto }`)،
  و**الشاشةُ الواحدة `min-height` لا `height` مقطوعًا**: كان ينضغط الصندوقُ
  ويبقى ما فيه على قدره فيقع على بطاقة الاقتباس ثم يُقصّ ما فاض. وتمريرةٌ
  قصيرة أهونُ من تراكب. وسطرُ الأعداد يتنحّى دون ٨٢٠ بكسلٍ ارتفاعًا، فتبقى
  الشاشةُ الواحدة على حالها في أكثر الشاشات.
- **الرأس يلتفّ عند صاحب المكتبة قبل الزائر** (`[data-owner]` دون ١٢٤٠):
  عنده فوق ما عنده أربعةُ عناصر، فيضيق به العرضُ وهو لا يزال واسعًا على
  الزائر.
- **أسماءُ المشاركين في بطاقة الكتاب في سطرٍ واحد** معطوفةً بالواو، كما
  تُكتب في صدر الكتاب. وكانت بعضُها تحت بعض فيقوم لكل اسمٍ سطر. ومتى اختلفت
  النطاقاتُ فُصل بينهم بفاصلةٍ لا بواو — هؤلاء ليسوا شركاء في عملٍ واحد.
- **كلُّ اسمٍ في لوح النسبة بابٌ إلى صفحته**، والدارُ كذلك في بيانات الطبعة:
  المشارِكُ يُربط بـ`person_id`، والدارُ بـ`publisher_id` — ولا يُكتب الرابطُ
  إلا إذا كان للمقصود صفحةٌ تُعرض فعلًا (أي إذا كان في القائمة التي جاءت من
  الخادم)، فلا يُوهَم القارئُ ببابٍ لا يفتح.

- **حقل الردمك يُشرَّط ويُتحقَّق منه ولا يُجلَب به شيء** (`lib/isbn.ts`):
  تُدخَل الشرطاتُ مع الكتابة، وتُحسب خانةُ التحقُّق فيُعلم أَصَحَّ الرقمُ أم
  حُرِّف فيه رقم، ويُقرأ من مجموعة التسجيل بلدُها. **وحدُّ التشريط البادئةُ
  والمجموعةُ وخانةُ التحقُّق لا غير**: الفصلُ بين رقم الناشر ورقم العنوان
  يحتاج جدولَ نطاقاتٍ يتغيّر مع كل تسجيل، ولا تُحفظ منه نسخةٌ ميّتة في
  الشيفرة — شرطةٌ في غير موضعها أسوأُ من تركها. والعشاريُّ يُقبل ويُعرض
  مقابلُه ثلاثيَّ عشرَ ليُؤخذ بضغطة. **والحكمُ خبرٌ لا حاجز**: الرقمُ
  المُحرَّف يُحفظ كما كُتب، فقد يكون الخطأ مطبوعًا على ظهر الكتاب نفسه.
  و**لا نداءَ لخادمٍ خارجيّ**: قواعدُ الردمك العالميّة المفتوحة تغطيتُها
  للعربيّة ضعيفة، وقرارُ صاحب المكتبة تركُها.

## الفوائد والمقتطفات — كنّاشُ المكتبة

القيدُ لا يُطلب من جهةٍ واحدة، فالصفحةُ بابٌ ذو أبواب لا سردًا تحت عناوين
الكتب: **السَّيْل** (القيود كلُّها بمُصفِّياتها)، و**الأبواب**، و**الأعلام**،
و**الكرّاسات**، و**النفائس**. ولكلٍّ موضعُه من الرابط (`#/perks/topics`)
فيُشارَك ويُعاد إليه، ولكلّ قيدٍ صفحتُه (`#/perk/:id`).

- **مصدرُ القيد إمّا كتابٌ من الفهرس وإمّا نصٌّ يُكتب**: `book_id` يجوز أن
  يكون `null`، وحينئذٍ يُقرأ `source` (عنوانٌ ومؤلِّفٌ وطبعة). فما قُرئ في
  مكتبةٍ عامّة أو في نسخةٍ إلكترونيّة أو في كتابٍ مستعار لا يضيع. ويُعلَّم
  في بطاقته بـ«ليس في المكتبة» — ولا يُقال «في المكتبة»، فذاك هو الأصل ههنا.
- **الأبوابُ تصنيفاتُ المكتبة نفسها** (`category`/`sub_category`) لا جدولٌ
  ثانٍ يُدار: الفائدةُ تُصنَّف بما تُصنَّف به الكتب.
- **الأعلامُ والكرّاساتُ والوسومُ تُشتقّ من القيود** لا من جداول، كما تُشتقّ
  الدواليبُ والسلاسلُ من الكتب. فالكرّاسةُ تقوم بأوّل قيدٍ يُكتب فيها.
- **النفاسة ثلاثُ نجومٍ، والثالثةُ هي «النفائس»**: صفةٌ واحدة لا صفتان
  (نجومٌ ووسمُ «نفيس»)، فلا يُسأل المُقيِّد عن الشيء مرَّتين.
- **تعليقُ المُقيِّد مفصولٌ عن النصّ** بشارةٍ وشريط (`comment`): كلامُه لا
  يُخلَط بكلام صاحب الكتاب، وهذا أوّلُ ما يُتحرَّى في النقل.
- **`PerkEditor` نموذجٌ واحد للإدخال والتعديل**، ويُفتح من الكنّاش ومن صفحة
  الكتاب جميعًا. وصندوقُ صفحة الكتاب عرضٌ لا نموذجُ إدخال، فلا يفترق نصفُ
  نموذجٍ هناك عن نموذجٍ تامٍّ هنا. و`PerkCard` بطاقةٌ واحدة في المواضع
  الثلاثة.
- **حقولُ القيد المستجدّة اختياريّةٌ في المخطّط** — في القاعدة قيودٌ كُتبت
  قبلها — وتُسدُّ في `api.toPerk` مرّةً واحدة، فلا تحرس الواجهةُ كلَّ حقلٍ
  في كل موضعٍ يقرؤه.
- **ترشيحُ السيل لا يُخلى بتبدُّل المسار**، وإنما يُخلى بضغط القارئ على بابٍ
  من التبويب (`openTab`): لو أُخلي في أثر المسار لمحا ما يضعه `pick` قبل
  الانتقال، فيبلغ القارئُ السيلَ بلا ترشيحٍ ولا يدري لِمَ.
- **الطبقاتُ كلُّها من `document.body`** (`createPortal` في `ui.Overlay`
  وفي `ImageViewer`): `#root` مكبَّرٌ بـ`zoom`، والتكبيرُ سياقٌ يتعلّق به
  الثابتُ (`position: fixed`) في بعض المتصفّحات، فلا يبلغ الظلُّ أطرافَ
  الشاشة وتبقى منه حواشٍ مكشوفة. وتكبيرُ القارئ يسري على `.overlay-sheet`
  وحده، وحدُّها `calc(100% / --ui-scale)` من الطبقة غيرِ المكبَّرة — فيبقى
  اللوحُ في حدّه مهما كبّر الخطّ. **ولا يُقاس لوحُ نافذةٍ بـ`vh` أبدًا.**
- **أعدادُ المكتبة داخل إطار الهبوط لا تحته** (`.frame-tally`): اللوحةُ
  صندوقٌ ارتفاعُه مضبوط، فما وُضع فيه لا يكلّف الصدرَ ارتفاعًا — وتبقى
  الصفحةُ شاشةً واحدةً بلا تمرير. وكانت صفًّا تحت الأزرار فأطالت الصدرَ حتى
  وقع على بطاقة الاقتباس.
- **أدواتُ صاحب المكتبة ليست في الترويسة** (`components/OwnerTools.tsx`):
  الترويسةُ طريقُ الزائر، ولا يُضيَّق على تبويبات المكتبة من أجل ثلاثة
  أزرارٍ لا يراها إلا واحد. ولها موضعان والقطعةُ واحدة: عمودٌ في فراغ
  الهبوط، ولوحٌ مطويٌّ في زاوية سائر الصفحات (`owner-dock`).
- **«الفوائد» بابٌ في الترويسة كسائر الأبواب**: القسمُ قائمٌ بنفسه له
  ترويستُه وأبوابُه، فمدخلُه من الرأس. واسمُه هناك «الفوائد» اختصارًا،
  والاسمُ التامّ في صدره. (وبقيت «السلاسل» و«المحقِّقون ونحوهم» خارج الرأس
  على أصلهما.)
- **صفحاتُ الأسماء الثلاث سجلٌّ واحدٌ بهيئتين** (`components/Roster.tsx`):
  المؤلِّفون ودُورُ النَّشْر و«المحقِّقون ونحوهم» — شبكةً للتصفُّح على مهل،
  وجدولًا للمسح السريع حين يكثر العدد. والاختيارُ يُحفظ في متصفّح القارئ لا
  في المكتبة، ولكلّ سجلٍّ اختيارُه. ومفتاحُ التنقُّل بين سجلَّي الأشخاص في
  صدرهما (`PeopleSwitch`): السجلُّ واحدٌ ووجهاه اثنان.
- **البحثُ يشمل القيودَ كما يشمل الكتب**: لوحةُ البحث السريع قسمان لكلٍّ
  صدرُه وعددُه، ومطابقةُ القيد في `search.matchPerk` — بنصّه وعنوانه
  وتعليقه وبابه وأعلامه ووسومه، ثم بعنوان مصدره ومؤلِّفه. وللسيل مُصفِّياتُه
  في الصفحة نفسها، وهي أوسع.
- **لوحةُ البحث فيها ما لكلّ لوحةِ بحث**: زرُّ إغلاقٍ ظاهر (لا Esc وحده ولا
  الضغطَ في الخارج — منهما ما لا يعرفه الزائر، ومنهما ما لا يبلغه بإصبعه)،
  وعددُ ما وُجد قبل النتائج، وإرشادُ البحث تحت الحقل: أنّ التشكيل لا يُشترط،
  وأنّ الهمزات سواء، وأنّ الكلمات تُطلب مجتمعةً على أيّ ترتيب. وهي خصالٌ في
  الخوارزمية قائمةٌ من قبل، غير أن أحدًا لم يكن يعلمها فلا يُنتفَع بها.
- **أنواعُ القيد يحرّرها صاحب المكتبة** من زرّ الإعدادات في ترويسة القسم:
  `perks.kind` نصٌّ حرّ لا اتّحادٌ مغلق، والقائمةُ في
  `library_settings.perk_kinds`. وتعديلُ اسم نوعٍ يُزامَن على قيوده في
  `catalog.renamePerkKind` — كما يُزامَن اسمُ المؤلِّف على كتبه. و`perkKindsOf`
  يُعيد إلى القائمة ما بقي في القيود من نوعٍ رُفع منها، فلا يسقط قيدٌ من
  السيل. والنوعُ الذي عليه قيودٌ لا يُحذف: حذفُه لا يمحوها، فيبقى الاسمُ
  ويظهر أنّ الحذف لم يقع.
- **الشعرُ في نصّ القيد يُعلَّم بالنجمة لا بمُحرِّرٍ ذي عُقَد**: `صدرٌ * عَجُز`
  بيتٌ، و`* شطر` شطرُ رجز — والنصُّ يبقى نصًّا يُنسخ ويُبحث فيه ويُحفظ كما
  هو. وما توالى من جنسٍ واحد جُمع في جدولٍ واحد. والعلامةُ لا تقع في النثر
  محفوفةً بفراغين، ونموذجُ القيد هو الذي يكتبها فلا يُطالَب بها الفاهرس.
- **مدُّ الأشطار بالتطويل منقولٌ عن مدوّنة صاحب المكتبة** (`lib/tatweel.ts`
  عن `static/site.js` هناك): لا يُوصَل بعد حرفٍ لا يتّصل بما بعده، ولا بين
  لامِ التعريف وألفها، ولا قبل همزةٍ مفردة، **ولفظُ الجلالة لا يُمدّ أبدًا**.
  والقياسُ بعد الرسم لا قبله، و`table-layout: fixed` شرطٌ له: لو تبع العرضُ
  النصَّ لدار القياسُ على نفسه.
- **القيدُ الذي لا كتاب له حكمُه حكمُ الباب كلِّه** في `library.perks`: إن
  عُرضت القيودُ عُرض معها، وإن حُجبت حُجب — إذ لا كتابَ له يُخفى.

## حال النشر

الموقع الحيّ على Convex منذ ١٣ أغسطس ٢٠٢٦، والحزمة المنشورة خلوٌ من
Supabase. ولا بيانات تُنقل: قاعدة Supabase القديمة لم يُفهرس فيها كتابٌ قطّ.

نشر الإنتاج `resilient-buffalo-631`: دوالُّه مدفوعة، و`seed:run` زرعه (رفّ،
٦ تصنيفات، إعدادات، صورة هبوط، اقتباس الجاحظ).

**مفتاح النشر يحتاج قراءة البيانات لا الدفع وحده**: `CONVEX_DEPLOY_KEY`
أُنشئ أوّلًا بصلاحية `deployment:deploy` فقط، فأخفق أوّلُ نشرٍ غيّر المخطّط
برسالة `You do not have permission ... (deployment:data:view)` — إذ يقرأ
Convex المستندات القائمة ليتحقّق من مطابقتها للمخطّط الجديد. ثم زِيدت
الصلاحيةُ، ونشرُ ١٥ أغسطس ٢٠٢٦ — وفيه حقولٌ مستجدّة ولفظُ حالةٍ جديد — مرّ
بـ«Schema validation complete»، فالمفتاح اليوم على ما يلزمه. ولا يُدفَع
المخطّط إلى الإنتاج وحده: الواجهة المنشورة تنادي الدوالّ القديمة، فينزلان
معًا عبر `convex deploy --cmd`.

**متغيّرات المصادقة تُضبط على كل نشرٍ على حدة**، ولا تنتقل معه:
`OWNER_EMAIL` و`JWT_PRIVATE_KEY` و`JWKS` و`SITE_URL`. غياب `JWT_PRIVATE_KEY`
عطّل الدخول على النشرين معًا، ورسالةُ الواجهة عنه «البريد أو كلمة السر غير
صحيحة» لأنها احتياطُ `describeSignInError` — فالسببُ الحقيقي في
`npx convex logs --prod` لا في الشاشة. ولكل نشرٍ زوجُ مفاتيحه.

`convex/auth.config.ts` لازمٌ كي يثق Convex برموزه: بدونه تُصدَر الجلسة ولا
يعرفها الخادم، فيظهر صاحب المكتبة زائرًا. ولا تُشغِّل `npx @convex-dev/auth`
لتوليده — تكتب فوق `convex/auth.ts` فيسقط قيد `OWNER_EMAIL`.

## ما لم يُنجز بعد

- **الفهرسة نفسها**: لا كتاب في قاعدة الإنتاج بعد.
- **سرّا `SUPABASE_URL` و`SUPABASE_ANON_KEY`** باقيان في أسرار المستودع بلا
  مستعمِل، ولا حاجة إليهما — يُحذفان.
- **الحيوية (reactivity)**: `api.ts` بقي نداءاتٍ إجرائية بـ `convex.query`
  حفاظًا على `library.tsx` كما هو. الانتقال إلى `useQuery` يجعل الصفحات
  تتحدّث وحدها، وهو تحسينٌ لاحقٌ لا شرطٌ للهجرة.
